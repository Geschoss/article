'use strict';

const path = require('path');
const fs = require('fs');

const entities = new Map();
'use strict';

const loader = require('./loader.js');

const methods = new Map();
const schame = loader(methods);

module.exports = schame;